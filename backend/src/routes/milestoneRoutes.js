import express from 'express'; import { getMilestones, createMilestone, updateMilestone, deleteMilestone } from '../controllers/milestoneController.js'; import { protect } from '../middleware/auth.js'; const router = express.Router(); router.get('/', getMilestones); router.post('/', protect, createMilestone); router.put('/:id', protect, updateMilestone); router.delete('/:id', protect, deleteMilestone); export default router;
